//for mapping snapshot of brew collection to our own class so we can create obj and it make data to handle easy its not necessary but to handle data easily i m doing this

class Brew {
  final String name;
  final String sugars;
  final int strength;

  Brew({this.name, this.sugars, this.strength});
}
